# Copy from Writers Room `.env.local` if you like — same variable names.
ANTHROPIC_API_KEY=sk-ant-...

# Spotify Web API (client credentials) — public playlists only for "playlist → books"
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
